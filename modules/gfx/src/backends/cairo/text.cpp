#include <cvx/gfx/text.hpp>
#include <iostream>
#include <set>

using namespace std ;
namespace cvx { namespace gfx {

std::vector<TextSpanGroup> StyledText::split()
{
    /*
    size_t start = 0, end = 0, len = text_.length() ;
    int current_span = -1 ;
    set<int> spans ;
    while ( end < len ) {
        int count = 0 ;
        for( int i=0 ; i<spans_.size() ; i++  ) {
            if ( end >= span->start() && end <= span->end() ) {
                spans.
                break ;
            }
            ++ count ;

        }

        if ( current_span >= 0 ) {
            spans.push_back(current_span) ;
            std::cout << start << ' ' << end-1 << endl;
            start = end ;
        }
        else {

        }
++end ;


    }
    */
}





}}
